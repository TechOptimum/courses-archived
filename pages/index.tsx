import Login from "../components/login";

import React from "react";

export default function HomePage() {
  return (
    <div>
      <Login loadingScreenCallback={undefined} />
      {/* hello */}
    </div>
  );
}
